{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "target": "es5",
    "types": [
      "jasmine",
      "node"
    ],
//    "allowJs": true,  // TODO uncomment when https://github.com/angular/angular/issues/21080 is resolved.
    "listEmittedFiles": true,
    "listFiles": true
  },
  "files": [
    "test.ts"
  ],
  "include": [
    "**/*.spec.ts",
    "**/*.d.ts",
    "../../src"
    // force transpile of whatwg-url from es6 to es5. Why do we even need to do this ?!?
//    "../../node_modules/whatwg-url" //  TODO uncomment when https://github.com/angular/angular/issues/21080 is resolved.
  ]
}
